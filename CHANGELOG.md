# 更新日志

本文件只记**使用者需要知道的三件事**：改了什么、**数据是否需要迁移**、怎么回退。

版本号规则：加 preset / 加工具 = minor；**域版本变化 = major**（它影响学习数据的兼容性）。

---

## [0.2.0] - 2026-09-26

### 新增

- **「苏格拉底教学模式」（第二个 agent preset，`presets/socratic/`）**：
  一步步启发式引导 —— 一次只问一个关键问题；答不上先降阶（例子 → 二选一）；追问深度到位才直讲并要求复述；
  用闪卡等小测验收；收束时插入到期复习并留下课堂记录。
- **`study_steps` 工具**：苏格拉底模式的"下一步由系统给" —— 返回环节、动作、
  `may_reveal_answer`（能不能给答案）、`must_retell`（是否必须先复述）、`question_budget`（恒为 1）。
- **`study_formats` 工具**：如实报告本机可解析的格式、所在层、**缺哪个依赖**，以及明确不支持哪些格式。
- **`study_wrap_up` 工具**：生成课堂记录（快照）、四类缺口归因、Markdown 落盘到 `.study/records/`。
- **按材料类型换教法**：条例 / 教材 / 讲义 / 真题 / 文章 / 答案解析 各有不同的追问重点、降阶阶梯与卡片偏好；
  类型判不准时会先问用户，不硬猜。
- **材料分类写进教材记录的 `sourceFormat` 复合值**（`pdf|regulation|strong`），旧值（无 `|`）仍可读 → **零 migration**。
- **`/socratic` 斜杠命令**：查看当前环节、材料形态与教学模式。
- 多格式导入的**能力可见与建议接地**：导入失败时会说明"本机实测缺什么"，而不是给模板话术。

### 修复（三个既有真 bug）

- **教材记录的形状与持久层从未一致**（`revision` 是对象 vs `z.string()`、`parseStatus` 缺失必填）：
  每次导入写下的都是结构不合法的行，重启时数据域按 schema 校验会拒开。
  现在 core 直接采用持久层形状，并有形状契约测试 + 宿主七格式读回双重回归。
- **活动教材的半截写入**：`setActiveTextbook` 只写 `activeTextbookKey`，而 global schema 要求 `schemaNote` 也在 →
  "导入过教材后，下次启动数据域打不开"。现在写全 global，并有跨 open 往返回归。
- **证据 id 用"幂等键长度"当摘要**：同一毫秒内写入的不同证据 id 相同 → 后写覆盖先写 →
  连写三次答错只剩两条，**追问深度永远到不了 3**（"深度 3 才直讲"在真实运行里失效）。
  现在用 FNV-1a 内容摘要 + 同毫秒递增序号。

### 数据迁移

**需要关注（但不需要手工操作）**：存储域版本 `1 → 2`（新增 `sessions` 表）。

- `STUDY_DOMAIN` 已声明 **`compatibleVersions: [1]`**，因此 v1 的记录会被 v2 正常读入，
  新增的 `sessions` 表对旧数据就是空表 —— **教材、证据、卡片、复习排期全部保留**。
- ⚠️ 这一行是升级安全的关键：本域是 `layout: 'per-record'`，**每个记录文件带版本戳**，
  后端读取只接受"当前版本 + compatibleVersions"，**不在集合里的戳会被丢弃（不是迁移），且不报错**。
  `plugin-host/tests/upgrade.test.ts` 用真实 `JsonStorageBackend` 正反两面验证了这一点。
- 回退到旧版本：旧记录的版本戳**不会被改写**（有断言），所以回退不会丢数据。

### 其他

- 门禁增强：`check:preset` 改为遍历 `presets/*`（此前硬编码 `study`，新模式是"绿灯但零覆盖"）；
  `check-encoding` 扫描范围扩到 `presets/`、`docs/` 与根文件（52 → 82 个文件）；
  `check:install` 增加"两个模式都在 + 卸载不留残骸 + 重装幂等"三条断言。
- 全量测试：133 → **248 通过 / 0 失败**。

---

## [0.1.0] - 2026-09-23

首个版本：把 Reasonix 伴学系统的教学框架迁移为 DSH bundle。

- 伴学模式（`presets/study`）：一对一陪读一份材料，工具面收窄到「读材料 + 伴学」。
- 八个伴学工具、`/study` 命令、稳定协议走 system prompt section、易变状态走 context。
- 零依赖纯函数域逻辑：掌握度（`masteryFromEvidence`，唯一正向判定）、追问深度状态机、
  四类卡片与 SRS 排期、带来源标注的统计。
- 多格式摄取（markdown / txt / html / pdf / docx / odt / epub / 图片），分层降级 + 失败尝试轨迹。
- 存储用 DSH storage domain（先落盘、再改内存、最后广播），数据域生命周期与 fiber 配对。
